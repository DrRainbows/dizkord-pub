# xAI / Grok API key — required for @grok chat, image generation, video generation
# Get one at https://console.x.ai
XAI_API_KEY=your-xai-api-key

# xAI Management API key — optional, enables Eve (voice agent) to remember conversations via Collections
# Get one at https://console.x.ai (separate from the regular API key)
XAI_MANAGEMENT_API_KEY=your-xai-management-api-key

# Cloudflare TURN server credentials — recommended for reliable voice behind NAT/firewalls
# Without these, voice uses STUN only (works on same network, fails behind strict NAT)
# Get a free TURN token at https://dash.cloudflare.com > Calls > TURN
CF_TURN_TOKEN_ID=your-cloudflare-turn-token-id
CF_TURN_API_TOKEN=your-cloudflare-turn-api-token
